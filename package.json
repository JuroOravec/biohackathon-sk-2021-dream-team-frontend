{
  "name": "omnibiome",
  "private": true,
  "scripts": {
    "build:web": "npm ci --also=dev && npx lerna bootstrap && npx lerna run --scope \"*/web\" --scope \"*/shared\" build"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}